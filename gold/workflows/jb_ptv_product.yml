dag:
  dag_id: jb_ptv_product
  schedule_interval: "0 0 * * "  # Runs daily at midnight
  default_args:
    owner: "airflow"
    start_date: "2023-10-01"
    retries: 1
    retry_delay: "5m"
tasks:
  - task_id: tsk_ptv_product
    operator: BashOperator
    bash_command: "echo 'Hello, Airflow!'"
